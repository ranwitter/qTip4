# Source layout

- `src/js`: JavaScript source files for the plugin build.
- `src/css`: CSS source files for the plugin build.

The Grunt build concatenates each folder into:

- `dist/jquery.qtip.js`
- `dist/jquery.qtip.css`
