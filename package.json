{
  "name": "qtip4-init",
  "version": "1.0.0",
  "private": true,
  "description": "Source + build setup for qTip4 output files",
  "scripts": {
    "build": "grunt build",
    "watch": "grunt watch"
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-concat": "^2.1.0",
    "grunt-contrib-watch": "^1.1.0"
  }
}
